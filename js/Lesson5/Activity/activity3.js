function getLastIndex(arr, index) {
    if (index) {
        // console.log("TRUE");
        for (let i = 0; i < arr.length; i++) {
            
        }
    }
    else {
        console.log("FALSE");
        return arr.pop();
    }
}
console.log(getLastIndex([02, 45, 56], 5));